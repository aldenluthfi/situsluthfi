export type FactObject = {
    text: string;
    source: string;
};

export type WritingObject = {
    id: string;
    title: string;
    description: string;
    createdAt: string;
    lastUpdated: string;
};